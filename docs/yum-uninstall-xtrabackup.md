# Uninstall Percona XtraBackup {{vers}} on Red Hat Enterprise Linux and CentOS

To completely uninstall Percona XtraBackup, remove all the installed packages:

```shell
yum remove percona-xtrabackup
```